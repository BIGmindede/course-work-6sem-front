import { Button, ButtonThemes } from '../../../shared/UI/Button/Button'

export default () => {
    return (
        <Button className={ButtonThemes.CLEAR}>
            toggle
        </Button>
    )
}